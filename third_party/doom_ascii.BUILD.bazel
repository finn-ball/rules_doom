load("@rules_cc//cc:defs.bzl", "cc_binary")

exports_files(["LICENSE"])

cc_binary(
    name = "ascii",
    srcs = glob([
        "src/*.c",
        "src/*.h",
    ]),
    defines = [
        "_DEFAULT_SOURCE",
        "VERSION=0.3.1",
    ],
    visibility = ["//visibility:public"],
)
